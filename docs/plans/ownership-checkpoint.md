# Ownership evolution checkpoint

## Current state

P0–P5 are deployed. P6 contributed development is qualified and deployed with public intake disabled. The detailed chronological evidence below preserves earlier checkpoints; the final section names the current next action.

- Approved direction: [plan](ownership-evolution.md), [desired state](../design/desired-state.md).
- Baseline: `7a3e951`; existing identities and approval states preserved.
- P0 complete: reuse permanent Agent identity; add explicit areas; do not reinterpret Assignment.Owner or existing schedule overlap rules; preserve approval and review semantics.
- P1 implemented locally: areas, versioned understanding, narrow timed verification obligations, constrained funding/grants, reviewed observations, restart-safe dispatch, minimal Areas UI and cancellation.
- P1 deployed through `make serve` on September 14. No real area, schedule, obligation or infrastructure action has been created by qualification. All 191 baseline assignment/run/decision/plan records retained identical identities, parents and states after startup; login responds. The pre-existing queued durability run remains in cheap preflight wait because `dig` is unavailable; it has not started inference. This is not an ownership regression.

## Evidence so far

Deterministic ownership tests passed, including SQLite close/reopen, duplicate registration/dispatch, knowledge across runs, stale/missing reviews, failed verification, cancellation, mid-run funding revocation, bounded creation and grant narrowing. The final `make verify` passed (39.955-second race suite; vet/build passed).

Desktop/phone browser qualification passed: area view, intent editing, cancellation, CSRF enforcement and no horizontal overflow. Screenshots: ignored `work/ui-ownership-desktop.png` and `work/ui-ownership-mobile.png`; mobile inspected.

Synthetic real-provider follow-up passed with Sol execution and Opus review. The first run recovered two tool errors; restricting observation recording to workers and making the obligation ID implicit removed that friction. The second run completed in 64.04 seconds with zero failed ADC tools and no human continuation. It used corrected owner context after the source task finished. This is an accelerated fixture, not a weeks-long real-world claim.

Independent Claude review passed after corrections. Findings fixed: credential screening on shared knowledge, superseded observation handling, completion at the activation limit, inherited old output, review discovery for operations-category observations, and funding removal during active verification. The reviewer withdrew its stale-attempt concern after inspecting in-place reassignment. Advisory native tools remain explicitly advisory, as authorized. Final report: ignored `work/ownership-independent-review.json`; logs `/tmp/adc-ownership-audit-corrections.log`, `/tmp/adc-ownership-live-final.log`, `/tmp/adc-ownership-verify-corrections.log`.

Consistent pre-rollout backup: `.adc/backups/before-ownership-20260914T021750Z.db` (0600); baseline metadata: ignored `work/ownership-before-deploy.json`. The new records are additive, but older binaries cannot safely execute active follow-up assignments without reconciliation.

## P2 qualification and deployment

P2 maintained knowledge, selected-revision discussion/correction, explicit history retrieval, observed/inferred understanding, and bounded discovery are deployed. Public intent preview includes only human-selected text. A linked public source can be checked by the owner; the stored agent-reported fingerprint exposes drift without importing external prose into intent. Public publication remains on the existing reviewed/authorized path; cadence-based refresh is later work.

`make verify` passed (40.974-second race suite, vet/build). Browser qualification passed with selected-passage correction and phone layout. The dormant-experiment live fixture passed in 72.03 seconds with Sol/Opus, retained the correction and proposed no maintenance. Real-source onboarding pilots for TrueNAS MCP and Snosi passed in isolated databases (188.07 seconds total). They used real README snapshots and explicitly retained unknown live state. The NAS pilot recovered one benign `adc_wait` race; the product pilot had no failed tools. Logs are `/tmp/adc-knowledge-source-pilots.log`, `/tmp/adc-knowledge-live.log`, `/tmp/adc-knowledge-browser-release.log`, `/tmp/adc-knowledge-verify-deploy.log`.

Independent Claude review passed after fixing exhausted-budget resume, late capacity checking, pending-note prioritization and count, and the human-brief newline. Final review of public-source drift passed; its low-severity suggestions were also addressed with regressions: retain a deferred drift-notification flag when the note queue is full, and label observations stale after the human changes expected public text/source. Report: ignored `work/knowledge-independent-review.json`; log `/tmp/adc-knowledge-audit-final.log`.

Pre-rollout consistent backup: `.adc/backups/before-knowledge-20260914T024947Z.db`; baseline in `work/knowledge-before-deploy.json`. There were no active provider runs. All 191 baseline tracked records retained IDs/parents/states after rollout; login responds. No production areas or schedules were populated with qualification data. Existing missing-`dig` preflight remains unchanged.

## P3 first coordination queue — deployed

Permanent-owner requests now retain lead/supervisor accountability, responses and history across assignments/restart. Cross-task delivery attaches only to already running/queued contexts; it does not bypass waits or create unfunded work. Three failed receiving attempts retain a blocker, and a later authorized owner run can answer a prior blocker with new evidence. Lead changes preserve spent attempts and original supervision. Shared Coordination UI provides bounded inspection and cancellation.

`make verify` passed (41.956-second race suite, vet/build). Browser qualification passed on desktop/phone with collapsed evidence and cancellation. Independent Claude re-review passed after fixing waiting-run wakeups, blocked stale claims, recipient-as-lead transfer and repeated routing scans. Report: `work/owner-coordination-independent-review.json`. A harmless empty-result retry edge noted by review was tightened before final verification.

Real Sol/Opus coordination passed after source completion (50.03 seconds). A second live fixture recovered a retained prior blocker through a later owner context and independent evidence review (76.03 seconds), without human continuation. The second recovered three ordinary tool errors, recorded in the backlog/P5 follow-through; it was not zero-friction. Logs: `/tmp/adc-owner-coordination-audit-corrections.log`, `/tmp/adc-owner-coordination-recovery-live.log`, `/tmp/adc-owner-coordination-browser.log`, `/tmp/adc-owner-coordination-verify-release.log`.

Deployment: `.adc/backups/before-coordination-20260914T031538Z.db` and `work/coordination-before-deploy.json` preserve the consistent baseline. No provider run was active. All 191 tracked records retained IDs/parents/states after restart; login responds. Existing Monday 09:00 America/New_York TrueNAS schedule remains active and unchanged. No real owner requests or areas were invented for qualification.

The queue's deliberately unfunded wait is not the completed background-attention experience. P4 must provide explicit standing scope/funding and a supervisor briefing, while preserving existing schedule semantics unless a human approves migration. Broader recovery accounting will evolve with that attention layer; the present counters cover requests and owner follow-up/discovery tasks.

## Next bounded action

Implement P4 bounded periodic assessment and the supervisor briefing using approved proposal/schedule machinery. Brian explicitly authorized replacing the TrueNAS review on September 14 ("feel free to replace the truenas review"). Preserve its Monday 09:00 America/New_York cadence, selected funding and approved read-only scope. Qualify and migrate without asking for this permission again. Existing P3 service stays running until qualification and backup are complete.


## P4 bounded attention — deployed

Approved recurring assessments now pin one to six permanent owner areas and retain separate scan/investigation/review activation budgets, a wall deadline, concurrent-worker limit and maximum proposals. Claims and the first-start timestamp commit atomically. Never-started occurrences expire after twice their execution window; exhausted stages reject new delegation, and exhausted cycles retain a pause without permanently suppressing the next approved scan. Existing legacy overlap semantics remain unchanged.

Owners persist provisional observations before finishing a scan. The supervisor clusters evidence, obtains independent review for area observations and a specialist-authored briefing, and may propose none or a bounded set of changes. Observation changes invalidate the briefing review. Prior decision notes are supplied, including declined work; renaming identical scope/evidence does not recreate it. Home shows current blockers before dated reviewed narrative with links to area/task/transcript evidence. Refreshing is read-only. Budget edits are available on proposal forms.

Qualification: final `make verify` passed (43.462-second race suite, vet/build), log `/tmp/adc-attention-verify-release.log`. Independent Claude review passed initially (197.03 seconds) and after recovery corrections (46.76 seconds), report `work/attention-independent-review.json`, log `/tmp/adc-attention-audit-corrections.log`. Browser desktop/phone passed with live disclosure preservation and drilldown; screenshots `work/ui-attention-{desktop,mobile}.png`, mobile inspected. Deterministic tests cover replacement/coalescing/restart, changed/superseded review evidence, funding/owner checks, budget/deadline/never-started stops, duplicate proposals and read-only rendering.

The first Sol/Opus fixture exhausted its scan budget rather than silently running forever. Completion guards and handoff instructions were tightened. A corrected cycle passed in 154.04 seconds; after clarifying optional proposal policy arguments, the final cycle passed in 134.04 seconds, combining NAS/hosting evidence into one proposal without a human continuation. It recovered one bounded-reference/brief error and two fast-child `adc_wait` errors; zero-friction is not claimed. Logs `/tmp/adc-attention-live-corrections.log` and `/tmp/adc-attention-live-final.log`. These are isolated synthetic facts, not real NAS health evidence.

Brian's explicit replacement authorization was applied to the real TrueNAS weekly review. New area `c9a3170829987cd39c966ab1f9cf7905` is owned by the existing Storage and Data Engineer, with intent taken from the exact already-approved scope. Schedule `61ee087ce75b0f6a5d4f08368fc7ae3f` is retained as replaced; new schedule `f0f8758bd09a5ff5a0cf086e15eb097a` is active, approval/provenance proposal `cb1199753b19915c971fc115dd0f5190`. Monday 09:00 America/New_York / next 2026-09-14T13:00:00Z, designated Copilot funding, original scope and MCP grants are unchanged; advisory authority narrows to observe. New limits: scan 3, investigation 8, review 6 activations; 20 execution minutes; 3 concurrent workers; at most 3 proposals. The existing Sol owner and Opus supervisor review models were verified available on that same account before migration. No occurrence was forced and no infrastructure action was performed.

Consistent backup `.adc/backups/before-attention-20260914T035047Z.db` (0600); baseline `work/attention-before-deploy.json`; migration receipt `work/truenas-attention-migration.json`. Service restarted through `make serve`; all 191 baseline tracked records retained identical ID/kind/parent/state, and login responds. The unrelated missing-`dig` preflight remains at zero activations. New schedule state is additive but not safe for execution by an older binary; pause/reconcile attention before downgrade.

## Next bounded action after P4

Proceed with P5 completion/review policy snapshots and domain-independent qualification. Preserve engineering cross-family review and current active approvals. Address the recorded fast-child wait race and oversized status context while implementing those handoffs. Event-driven attention and automatic recovery for every unlinked retained blocker remain follow-through, not implied by the new briefing. No further authorization is needed to continue the approved plan.


## P5 completion policies and domain qualification — deployed

Human areas now select reviewed (default) or routine observed-evidence completion. New assignments/proposals can link an area and save a versioned completion policy. Existing work, legacy schedules and linked follow-up funding retain their earlier policy; nil historical policy is always reviewed. Code artifacts, execution-plan steps and P4 assessments always keep independent review. Routine owners can record `adc_evidence`, retain a later obligation, and directly record its `adc_obligation_result` when due, without artificial QA/documents. No real area was switched to routine mode.

The same slice makes already-finished-child `adc_wait` a successful continuation (without ending the active provider turn), offers optional focused `adc_status` views, hides candidate-review tools outside planned workers, and restricts evidence-author tools to roles that can use them. A verification assignment cannot finish solely on generic reviewed prose: its linked observation and required review must exist. Attention-budget proposal edits now copy the policy before editing so previous revision history is unchanged; an explicit one-off refinement removes recurrence policy and stays pending.

Validation: `make verify` passed (44.934-second race suite, vet/build), `/tmp/adc-completion-verify-deploy.log`. Independent Claude audit PASS at 62.98 seconds and focused final PASS at 30.76 seconds, report `work/completion-policy-independent-review.json`, `/tmp/adc-completion-audit-final.log`. Targeted regressions additionally cover proposal history aliasing, completion tool visibility, missing linked observation, historical policy preservation, current evidence, code gates and focused-read boundaries. Browser policy editing, CSRF, active snapshot preservation and desktop/phone layout passed; screenshots `work/ui-completion-{desktop,mobile}.png`, phone inspected.

Live scenarios: routine family confirmation and later real timed verification PASS in 98.03 seconds with zero failed tools and no QA/doc artifact (`/tmp/adc-completion-family-live-clock.log`). The initial accelerated family fixture exposed its own future-clock mismatch; the live fixture now waits for an actual short due time. Reviewed synthetic product/delivery investigation PASS in 143.03 seconds with retained owner understanding (`/tmp/adc-completion-cross-resource.log`); it recovered two evidence/reference-format errors. A strict continuity fixture exposed missing durable observation despite reviewed prose; the completion guard and handoff instructions fixed this. Synthetic NAS verification PASS in 68.04 seconds, then PASS with a real short timer in 91.04 seconds (`/tmp/adc-completion-nas-real-timer.log`); one candidate-review tool misuse was then eliminated by catalog filtering. These prove controlled lifecycle behavior, not real NAS health, family arrangements or hosting measurements. Deterministic NVIDIA-style experiment coverage leaves hardware verification owed and rejects an unrelated external PASS from closing it or changing intent.

Deployment: consistent backup `.adc/backups/before-completion-policy-20260914T042117Z.db`; baseline `work/completion-before-deploy.json`. `make serve` restarted; all 191 prior tracked records kept identical IDs/kinds/parents/states, login responds. TrueNAS's replacement remains active for 2026-09-14T13:00:00Z with zero occurrences so far and default reviewed mode. Existing missing-`dig` preflight remains unchanged. No production area, team, schedule, permissions or subscription selection was changed during P5 rollout.

## Next bounded action after P5

P6: inspect Bluefin/Snowcat reference flows, then implement deliberately public work packets and controlled contribution admission with demonstrated hostile-input isolation before enabling anonymous intake. Public exposure and any production eligibility/funding policy need a concrete reviewed configuration; do not infer that all organization work is public. Begin with the already approved development contribution shape; external reviews still require internally owned admission, and supervision/ownership/coordination are never contributed roles. P7 federation remains afterward. Ordinary source implementation remains authorized without another continue prompt.


## P6 contributed development — deployed, public intake disabled

The first contributed-development path has human-approved public queue policy, permanent-owner offers, explicit public text-source packets, anonymous HTTP claims with automatic receipts, bounded submission/replay handling and internally owned admission. The reviewer receives only the public packet/candidate and two tools. Candidate commands use a distinct offline bubblewrap/systemd profile with aggregate CPU/memory/process/time limits and disposable filesystems. Protected owners may import admitted source into a fresh directory; normal integration, code review and publication authority remain required. Source URL/revision assertions and semantic disclosure remain the authorized owner's responsibility, not a proved source-attestation boundary. See [the protocol and operating limits](../design/contributions.md).

Owners can wait once for up to an hour, release their slot and resume for results or timeout without human continuation. Received/reviewing contributions survive wait expiry. Queue and packet claim rate limits recover in a rolling hour; release adds a short cooldown. Stopped admission returns responsibility to its owner. Pausing public intake does not prevent importing an already admitted result into an active protected source assignment.

Qualification: final `make verify` passed (47.860-second race suite, vet/build), log `/tmp/adc-contribution-verify-final.log`. Desktop/phone policy approval and CSRF passed; screenshots `work/ui-contribution-{desktop,mobile}.png`, mobile inspected and checkbox/button styling corrected. Final browser log `/tmp/adc-contribution-browser-final.log`. Deterministic coverage includes atomic claim races, replay budget, SQLite restart/no duplicate review, revocation, durable owner wakeup, expired waits during active admission, fractional expiry, bounded resource exhaustion, no ordinary tools in admission, guarded imports and no original-task completion/publication change.

Real unaffiliated Sol contribution and internal Opus admission passed in 31.66 seconds with zero failed ADC tools: an external false PASS with prompt-injection/exfiltration instructions was independently rejected on observed failing criteria; a correct candidate was independently tested and admitted. HTTP claim, submission and receipt-bound outcome used no ADC membership or manually issued credential. It is a labeled synthetic calculator fixture, not production repository validation. Log `/tmp/adc-contribution-live.log`.

The first independent audit timed out after excessive source browsing and produced no verdict. The source packet was tightened; the next audit found five concrete recovery/input defects (139.73 seconds), all corrected with regressions. Independent focused re-review then passed (43.74 seconds), report `work/contribution-independent-review.json`, log `/tmp/adc-contribution-audit-corrections.log`. It confirmed the internal tool/context isolation and the corrections; it did not claim proof against kernel vulnerabilities or semantic source disclosure errors.

The default listener returns 404 for public routes. `make serve ADC_PUBLIC_CONTRIBUTIONS=true` explicitly enables them only when a real queue scope/source and review funding have been chosen. No production public queue, disclosure, external submission, infrastructure mutation or donated work was created by qualification. A production pilot needs Brian's choice of public source and finite internal review budget. P7 federation remains after this milestone. Queue edit/top-up/archive, broader dependency images, binary artifacts, optional external review contributions, source attestation and a packaged contributor client remain follow-through.


P6 rollout: consistent backup `.adc/backups/before-contributions-20260914T045837Z.db` (0600), baseline `work/contribution-before-deploy.json`. No provider run was active. Restarted through `make serve` with `-public-contributions=false`; all 191 assignment/run/decision/execution-plan records retain identical IDs/kinds/parents/states. Login returns 200, public queue requests return 404. The replacement TrueNAS schedule remains active for 2026-09-14T13:00:00Z. The unrelated missing-`dig` preflight remains unchanged. No production contribution policy exists. Before a downgrade, disable intake and pause/reconcile any new admission runs; do not let an older binary execute the new task kind.

## Current next action

Brian selected Updex as the first contribution pilot on September 14. Prepare one bounded Updex work packet, establish the accountable owner and internal reviewer, and qualify its actual build/test requirements in the isolated admission environment. Three internal evaluations were recommended as the initial budget; record the concrete funding policy before enabling intake. Repository selection does not expose every Frostyard task or authorize merge/publication. Qualify the actual deployment address/runtime before enabling public intake. P7 supervisor-only federation remains the next numbered implementation phase; its pairing/disclosure policy is separate from anonymous contributed work. Remaining event-driven owner wakeups and accountable recovery are still tracked in the backlog and must not be described as complete.


## Updex pilot — passed, runtime deployed (September 14)

The bounded real-source pilot succeeded on the first candidate: Sol contributed `version/example_test.go`, Opus independently inspected and tested it offline, ADC resumed the owner and imported the admitted candidate. One of three available qualification evaluations was used, two validation commands, zero failed ADC tools; 55.81 seconds. The exact admitted file then passed Updex's full pinned `make ci`, documentation integrity and native CLI smoke gates. Local Updex commit `6d435c24baac24776b7ca17c58592fcf0ef98ae5` on `adc/updex-contribution-examples` is ready for publication review. No PR, push, merge or release occurred. Details and limits: [pilot report](../testing/updex-contribution-pilot.md). The independent reviewer verified package scope; the development agent performed full-repository integration, so unattended end-to-end GitHub delivery is not claimed.

ADC now accepts an optional installation-curated Go runtime, pinned into human queue policy and public packets. Snapshots copy and hash the bytes actually mounted read-only. Runtime withdrawal disables intake before spending further evaluations; unchanged basic queues retain their old profile. Exact Go 1.26.7 and one required public module are installed for this pilot. Final independent Claude review PASS (58.53 seconds) after correcting the withdrawal/budget finding. Full final `make verify` PASS (48.081-second race suite, vet/build), `/tmp/adc-updex-verify-final.log`. Desktop/phone queue selection pins the chosen runtime and preserves CSRF, PASS in 0.73 seconds; `/tmp/adc-updex-browser.log`, mobile screenshot inspected.

Development serving now runs under transient `adc-development.service`, independent of the initiating tool/terminal session. Added reproducible `make start`, `stop`, `status`, `logs`; foreground `serve`/verified `run` remain. This is not reboot autostart.

Deployment: no provider run was active. Stopped the user service; consistent backup `.adc/backups/before-updex-pilot-20260914T184007Z.db` (0600) and baseline `work/updex-before-deploy.json`. Restarted with `make start`. All **197** tracked assignment/run/decision/execution-plan identities and states match the baseline. Login returns 200; running executable matches verified binary and has the pinned runtime configured. Production public routes remain 404. No production queue, permanent worker, provider funding policy or permissions were changed by the pilot. The existing missing-dig blocker remains unchanged.

Next decision: authorize publication of the concrete qualified Updex draft PR (description prepared in `work/updex-pilot-pr.md`), without merge/release authority. A real production anonymous queue and externally reachable address remain separate deployment work; the successful loopback qualification does not silently enable them. Event-driven owner recovery, sustainable queue management and P7 federation remain on the plan. New pilot friction/scratch follow-through is recorded in the backlog.


### Updex publication authorized and performed

Brian answered yes to opening the concrete draft PR. [frostyard/updex#417](https://github.com/frostyard/updex/pull/417) is open as a draft from his existing fork, branch `adc/updex-contribution-examples`. The final rubric check added the mandated coverage ratchet from 86.2% to observed 86.4% because the new parse-error example covers an additional statement. `make ci` passed again with the raised baseline; commit `ce95cb0` follows the admitted example commit `6d435c2`. No example code changed after independent admission. This closes the prior publication decision; merge/release and production anonymous exposure remain unauthorized. GitHub CI status is authoritative on the PR.

## Live localhost Updex queue — deployed, integration in progress

Brian selected this machine/localhost for the first live queue. Using the authenticated UI, created the Updex area `1c3645e94cb4cfcf5f2106da658846e2` under the existing Frostyard Product Engineer and approved queue `146ce674112a5ef2d20215bc00276aac`: one bounded public-source Updex docs/test contribution, existing Copilot funding, existing Opus reviewer, pinned Go runtime and three lifetime admission evaluations. No permanent agent, subscription, MCP grant or recurring schedule was added. The server explicitly binds 127.0.0.1:8789 with intake enabled; public internet deployment is not authorized or tested.

The actual owner assignment `564e5271fe216456e0976cc23948b10f` investigated current main and offered packet `548a1e1c11f54a7eaf69fadce231d16e` for version fuzz invariants, avoiding the examples already present from #417. A separate real Sol donor process received only the public queue URL, claimed/submitted over HTTP without production database or membership access, and completed in 246.81 seconds. Contribution `8df4cd78ef6ef533cca49340066540f6` passed independent offline Opus admission, including two 25-second fuzz runs. One evaluation is spent; two remain. This is production scheduler evidence, not an invented UI fixture or a packaged public contributor CLI.

The run exposed two product defects: provider offloading of oversized tool responses into inaccessible private /tmp, and refusal to rejoin a contribution wait after steering. ADC now bounds encoded inline results with explicit truncation guidance and preserves exit status; incomplete admission previews cannot newly satisfy validation. Rejoining retains the original deadline, and terminal/expired waits return continuation. Independent Claude review passed after linear UTF-8 handling and stale WaitRun corrections (final focused PASS 23.78 seconds). Final `make verify` passed (48.388-second race suite, vet/build). Reports: `work/tool-output-independent-review.json`, `/tmp/adc-live-wait-final-review.log`, `/tmp/adc-live-queue-verify-final.log`.

Deployment/recovery: consistent backup `.adc/backups/before-live-wait-repair-20260914T191121Z.db`; baseline `work/live-wait-before.json`. The obsolete runtime-only human decision `61d64731cfe4ff095c95f3b8e5f41c05` was explicitly superseded, the existing blocked owner queued with the admitted result, and its stale wait cleared in one guarded transaction. This operator repair did not fabricate a human approval, reset usage or grant publication. `make start` now serves the verified build under `adc-development.service`; login/public queue return 200, listener is loopback only. Of 202 baseline tracked records, only the intended owner, assignment and obsolete decision changed states; no unrelated identity/parent/state changed. Receipt: `work/live-wait-after.json`.

The resumed owner imported the admitted candidate, bootstrapped checksum-verified pinned public tools in its protected workspace, passed full Updex CI and raised coverage from 86.4% to 86.5%. Registered commit `85967a7ca7c4d53945d7edcd3fb2bcd32e096723`. Its attempt to finish directly encountered the supervisor-code-delivery guard; it recovered by delegating worker `511010d2d636bf1f4c2ee78bfcfee28e`. Final integration/review is still in progress. The initial output steering and runtime repair were operator interventions; zero-intervention completion is not claimed. Publication of this second candidate remains a concrete later decision, with no merge/release authority.

### Reviewed supervisor code handoff — deployed

The finalizing worker completed and Opus independently passed the exact integrated commit (review `b3377956deb4c2c2db88f85b71694ede`), including full CI, independent base/head coverage measurements and 11,770,752 fuzz executions. The supervisor then hit a second completion failure: the old root-code guard remained unconditional even after the required worker/review handoff. Fixed it to accept only exact retained commits adopted by clean completed direct workers with a current final PASS independent of both author model families. Original evidence and all other completion/authority gates remain intact. Regression tests exercise real Git trees through adc_finish, including dirty/stale/mismatched/incomplete/same-family and newer-changes rejection.

Independent Claude audit PASS in 26.94 seconds; `make verify` PASS in 48.823 seconds (race suite, vet/build). Reports: `work/supervisor-code-handoff-independent-review.json`, `/tmp/adc-code-handoff-audit.log`, `/tmp/adc-code-handoff-verify.log`. Graceful service stop queued the existing active owner; backup `.adc/backups/before-code-handoff-20260914T192350Z.db` and 204-record baseline `work/code-handoff-before.json`. `make start` resumed that same owner automatically. All baseline IDs/parents remained; only its queued→running transition occurred. The listener is still 127.0.0.1:8789 and the actual ADC process executable matches the verified binary. No direct database repair was needed for this second recovery.

A visible steering note asks the owner to finish the separate make fmt/docs-integrity checks required by Updex AGENTS.md, without creating more copies to evade the former guard. This is another operator correction of an omitted gate, not autonomous gate discovery. The final publication packet remains local while those checks finish.

### Current checkpoint — live localhost pilot complete

At 19:25 UTC the same accountable owner completed through adc_finish and assignment `564e5271fe216456e0976cc23948b10f` became ready, with no pending decision or review. Formatting left commit `85967a7ca7c4d53945d7edcd3fb2bcd32e096723` unchanged. The owner installed official checksum-verified Node v22.23.2 within its workspace and passed docs integrity: 20 documents, 278 links, 9 symlinks. A missing-awk command was corrected to the available mawk without a human intervention. All final code evidence and the independent review remain attached to their original runs.

The live queue, URL-only donor, internal offline admission and final repository handoff have now all been exercised against the real scheduler. The two product recovery deployments and explicit steering above remain part of the evidence; this was not zero-intervention. No additional permanent worker, account, permission, schedule or public exposure was added. Queue budget remains one spent of three. The public address is localhost only.

Next meaningful decision: publication of the concrete second Updex draft PR, `test(version): fuzz pattern and comparison invariants`; prepared body `work/updex-fuzz-pr.md`, diff `work/updex-fuzz.patch`. This is a separate change from the already merged #417. No merge/release is authorized. After the bounded pilot, sustained queue management/runtime scratch and earlier handoff/gate guidance remain follow-through; P7 supervisor-only federation remains the next numbered phase, with pairing/disclosure policy still to choose.

### Second Updex publication — authorized and performed

Brian approved publication of the concrete fuzz-test candidate. Draft PR [frostyard/updex#418](https://github.com/frostyard/updex/pull/418), `test(version): fuzz pattern and comparison invariants`, now targets main from `bketelsen:adc/updex-version-fuzz`. Its head is the unchanged independently reviewed commit `85967a7ca7c4d53945d7edcd3fb2bcd32e096723`; upstream main still matched the qualified base at publication. This closes the prior publication decision. No merge or release was performed. This development session performed the GitHub delivery; the completed ADC assignment supplied the reviewed artifact. GitHub checks remain authoritative on the PR.

The host Git credential helper initially invoked its mise shim inside the protected checkout and refused the untrusted local mise configuration. Delivery used a command-scoped helper pointing at the already-installed gh binary; no mise trust, credential grants or persistent Git configuration changed.

Publication verification: all 12 executed GitHub checks on #418 passed, including unit/E2E/race, both builds, exact-pin lint/verify, docs, security and release configuration. The automated Copilot review request was skipped for the draft; the recorded independent Opus review remains the pre-publication review evidence. Head still equals the qualified artifact. No merge occurred.

## ADC-owned draft PR delivery — Chairlift, in progress

Brian explicitly requested that ADC itself open a draft PR and supplied `frostyard/chairlift` → Updex 2.0.0 as the useful target. Created real assignment `758ef4008e4211e2b3ae58bdb244dca9` through the authenticated UI, owned by existing Frostyard CTO `624149ef8fb0806150cfa37ba0cd02f8` (run), using existing Copilot funding and the unchanged organization-default advisory environment. No new connection, credentials, model account or permanent agent was granted. The CTO created S1 discovery, S2 implementation, S3 draft publication/observation with existing independent Opus review. The development session observes/repairs ADC friction; it must not execute this task's GitHub push/PR on behalf of ADC and claim the goal achieved.

Brian clarified inside ADC at 19:34 UTC: push to a branch directly in `frostyard/chairlift` and create the PR there. The CTO propagated that correction to discovery. This supersedes the initial fork wording in the task brief. Scope includes focused compatibility changes, repository gates, independent exact-artifact review, push and draft PR creation/CI observation. No merge/release/deployment or unrelated repository changes are authorized. Publication is explicit in the human brief; the current UI only records its structured Publication flag on a decision response, which remains an affordance gap to handle within already-given authority if needed.

Discovery independently confirmed `github.com/frostyard/updex/v2@v2.0.0` cannot resolve because the released tag declares module `github.com/frostyard/updex` without `/v2`. The architect is determining the migration implications; no workaround or upstream release authority has been invented. This is a real dependency finding, not an ADC tool-policy blocker. Await the CTO's concrete recommendation if a product/release decision is required.

### Steering correction — let the CTO propose upstream work

The development session prematurely directed the CTO to implement/publish the Updex module fix after the dependency finding. Brian explicitly corrected this: let ADC decide whether to propose the work first. The execution/publication instruction has been retracted through a visible steering message. The original Chairlift objective remains; Updex implementation/publication is NOT approved. Read-only discovery already delegated may finish, and the CTO should assess the blocker through its normal workflow. Do not approve the pending upstream publication checkpoint on Brian's behalf; that request arose from the withdrawn development instruction. Let Brian review any actual proposal before upstream work starts. Further coaching of the CTO would undermine this observation.

### September 15 — bounded supervisor recovery and removable human requirements

Brian asked to repair ADC's follow-through after repeated Repogen stalls. Confirmed two independent failure modes: waitProgress counted unasked human-evidence placeholders as progress, and full/summary status failed to give the supervisor a usable current blocker/prior-answer packet. The live task also contains newer human instructions reopening A0 and removing the disproportionate A1 requirement; those must take precedence over the earlier hold in diagnostics.

Implemented a persisted per-blocker wake fingerprint, descendant-aware wait progress, paginated coordination/prior-decision views and exact step lookup, plus an explicit own-pending-decision withdrawal tool. No recovery path approves an action, resumes a held worker, rewrites an answered decision or resets completed work. Added human-only removal of an idle step's human-evidence requirement, retaining historical scope and all other gates, including independent review. The supervisor-proposed/conversational amendment path remains a documented priority; do not claim that chat automatically changes a frozen plan.

Independent Claude review PASS (53.95 seconds); applied its two small suggestions to clear old worker retry delays and preserve pending supervisor-requested executor approvals. Focused lifecycle/scope/boundary tests and a real desktop/phone browser fixture passed. Final verification and deployment are recorded below when completed.

Deployment completed: final `make verify` passed vet/race/build (49.194-second race suite); phone amendment fixture passed (0.53 seconds), diff whitespace check passed. The idle service stopped cleanly and restarted through `make start` on 127.0.0.1:8789. Running executable SHA256 `5e6ba2b2b82958515c7cadc3d15b3869c64f828c942708f0363b8714eb3c2565` matches the verified build. The snapshot comparison found no changes to completed runs, ready assignments or answered decisions.

Administrative application of existing human direction (not an autonomous ADC action): after backup `.adc/backups/before-coordination-20260915T013911Z.db`, used the new amendment function to apply authenticated Brian event 43636, “Remove this annoying requirement for A1.” Verified exact event/task/org, current membership and plan revision before mutation. Repogen plan revision 1 → 2 removes only `A1-dispositions`, retaining old plan/evidence, other gates and review. No fake disposition evidence was recorded. The one-off administrative script and output are retained in ignored `work/coordination_admin_repair.go.txt` and `/tmp/adc-coordination-admin-repair.log`; it is not a production tool or test fixture. After restart the CTO and A1 worker actually resumed and used the new coordination/decisions/step views. A0's worker hold was not silently lifted by recovery; its already-authorized preparation remains for the CTO to repair.

Follow-up observed during live qualification: the amended A1 author began materializing unchanged repository baselines solely to satisfy its frozen integration contract, then QA began reviewing that no-op. This is not the desired outcome. Added whole-step human omission (`PlanStep.Omission`) with separate graph accounting; do not mark a retired step complete or invent passing artifacts. Membership/CSRF/revision checks remain, active run goroutines/access requests/started descendants prevent unsafe edits, completed history is preserved, and no output from an omitted prerequisite can be consumed. A pending supervisor question no longer freezes unrelated authorized active-plan dispatch. Independent omission review passed (88.19s); implemented its lifecycle suggestions and requested a focused final re-review including the dispatch change. Updated phone browser qualification passed in 0.649s.

Final omission review PASS (57.00s), including the scheduling change. One suggested root-decision concern was checked against the actual `pendingDecision` helper: both use pending `d.Run == root.ID`, so no mismatch exists. Durable-wait cancellation was separately verified in `dispatchWaits`/`applyWaitResult`: superseded/cancelled run checks cancel observations and reject late results. The previous regression expecting global graph freeze on a supervisor question was updated to the agreed unaffected-work behavior; blocked-root and pre-dispatch funding checks remain covered separately.

Final rollout: `make verify` passed vet/race/build (49.088s), including the updated unaffected-work regression. Stopped the service cleanly; backed up to `.adc/backups/before-omission-20260915T015132Z.db`; applied the same authenticated Brian event 43636 through `omitPlanStep` with exact plan revision 2 and member identity checks. This administrative application—not an autonomous agent amendment—advances Repogen to revision 3 and explicitly omits A1. Its unused author/reviewer are cancelled and superseded; no repository output is admitted from A1. Historical records remain. One-off script/output: ignored `work/omission_admin_repair.go.txt`, `/tmp/adc-omission-admin-repair.log`.

Restarted through `make start`; HTTP 200 and running executable SHA256 `b1b0c23983c60a42d6ecb846763ff2f799babb0d0c7fafb3ef02640256d5f35f` match. Verified actual live progression: G1 run `17fff9d448e8cb40322c80cc622a4110` and G3 run `593821b86362ec10b3197a833e529e1b` are RUNNING despite the preserved pending A0 question `b23715a4749727d1c30b5ddf86e638ed`. A0 independently discovered valid signed repository metadata changed since its original frozen baseline; that question was neither approved nor dismissed by development work. The original completed/ready/answered state snapshot is unchanged. Remaining usability priority: supervisor-authored bounded plan amendment through conversation, without requiring the human to repeat an existing direction.

### Conversational area creation

Brian requested a chat-based path in “Create an area,” similar to team creation. Added a temporary area designer using the human's selected account/model and the existing restricted proposal-conversation runtime. It can inspect existing areas and propose one structured area with an existing permanent owner, intent/boundaries and completion mode. No permanent area or worker, tool access, discovery, obligation or schedule is created before approval. Pending revisions replace the approval ID; reject/refine create no area. Approve revalidates and atomically creates the area plus resolved decision/completed conversation, then links the human to the saved area. Direct form creation/editing remains available.

Validation: real isolated Sol proposal PASS (6.02s); desktop/phone browser start→refine→approve→saved-area PASS (5.20s); independent Claude review PASS (23.85s); lifecycle and account/owner/tool boundary tests passed. Temporary guide and assignment creation share a transaction; area creation and approval share a transaction. The review's duplicate-name timing note was checked: the action router holds Store.mu for validation and commit. Full verification/deployment follows below. No real organization area was created during qualification.

Area-chat rollout completed: full `make verify` PASS (50.342s race suite), clean diff check, backup `.adc/backups/before-area-chat-20260915T020307Z.db`, clean stop/start through Make targets. HTTP 200; running executable matches verified SHA256 `d03e1059d77514d1595f357c4afb46faad598b94476fb296367eca6447fce242`. Completed/ready/answered records remained unchanged; previously active assignments resumed through normal queued/running/waiting recovery. Feature is available under Areas → Create an area → Start with a conversation. No real organization area was created by this development session.
